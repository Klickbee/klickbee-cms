import { Plus } from "lucide-react";

export default function EmptyChildrenPlaceholder() {
	return (
		<div className={"h-[50px] flex items-center justify-center"}>
			<Plus size={18} />
		</div>
	);
}
